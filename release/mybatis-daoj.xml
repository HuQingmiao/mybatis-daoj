<?xml version="1.0" encoding="UTF-8"?>
<!-- Created by HuQingmiao -->

<daoj>
    <dataSource>
        <property name="jdbcDriver" value="com.mysql.jdbc.Driver"/>
        <property name="jdbcUrl" value="jdbc:mysql://10.75.204.32:3306/test"/>
        <property name="username" value="root"/>
        <property name="password" value="root"/>
    </dataSource>


    <params>
        <!-- 指定实体类基类, 默认为 BasicVo -->
        <property name="basicEntity" value="com.github.walker.common.BasicVo"/>

        <!-- 指定DAO基类, 默认为 BasicDao -->
        <property name="basicDao" value="com.github.walker.common.BasicDao"/>

        <!-- 设定生成后的实体类所在的包 -->
        <property name="entityPackage" value="com.github.walker.tifa.auxiliary.vo"/>

        <!-- 设定生成后的DAO接口所在的包 -->
        <property name="daoPackage" value="com.github.walker.tifa.auxiliary.dao"/>


        <!-- 设定代码生成的输出目录, 支持相对路径或绝对路径 -->
        <property name="outputDirc" value="generated-files"/>

        <!-- 列出要生成的表名, 多个表可以逗号(',')分隔。 注意，数据库表必须指定了主键列。 -->
        <property name="tables" value="user_log"/>

    </params>

</daoj>